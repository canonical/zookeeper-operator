# In Juju 3 this will be easier to copy
get-zookeeper-properties:
  description: |
    Action to retrieve the current zookeeper.properties
get-snap-apps:
  description: |
    Action to retrieve the available commands within the kafka snap
get-zookeeper-leader-logs:
  description: |
    Action to retrieve the server.log output from the current zookeeper leader

